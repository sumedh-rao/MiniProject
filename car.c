#include "fun.h"
#include <stdio.h>
int not = 0, noc = 0, nor = 0, nob = 0, amount = 0, count = 0;
void car()
{
    printf("\nEntry Successful");
    noc++;
    amount = amount + 100;
    count++;
}